# 2026-09-03

## Shared AI across Red Alert 2 and World War III

- Added a native game selector when both games are installed. Switching games
  keeps the wrapper-owned AI service alive and remembers the selection.
- Reused the assistant, model settings, AUTO and remappable voice controls in
  the RA2 integration. The HUD now always displays the configured Ask shortcut.
- Moved voice-button translations into the shared UI and kept game-selection
  buttons above the full-screen input-blocking menu background.
- Added the active mod and observed actor display names to the bridge so RA2
  GIs/Conscripts are not described using Red Alert 1 names.
- Standardized bridge cell coordinates on map-storage coordinates, including
  orders, rally points, spatial layers and observations on isometric maps.
- Preserved the World War III faction/capability catalogue without alteration.
  RA2 custom-faction ports, original campaigns and Yuri's Revenge are not part
  of this integration.

## Verification

- Release build: no compiler warnings or errors.
- Engine tests: 513 passed; two pre-existing skips.
- Companion integration tests and all 27 playable RA2 map/AUTO startup smoke
  checks are run by the product repository. Live Soviet AUTO deployed the MCV,
  built power and a refinery, and continued base production.
- Packaged Mac UI and release-signature verification remain product-level gates.

## RA2 live-match crash correction

- Live Allied testing caught a crash when GIs entered the match. Observations
  now support multiple conditional attack modes, use the active weapon ranges,
  and avoid inspecting unseen enemy weapons for threat coverage.
- Added a regression guard against single-attack-trait assumptions. Engine
  verification now passes 514 tests with the same two existing skips.
- The first signed integrated preview is not suitable for installation; it
  must be replaced by a rebuilt package containing this correction.
- Clarified the RA2 startup message: it counts the native countries and
  explicitly identifies the shared AI controls, instead of misleadingly saying
  that zero capabilities are enabled. Custom packs remain in World War III.
